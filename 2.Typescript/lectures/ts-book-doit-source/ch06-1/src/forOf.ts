const numArray: number[] = [1, 2, 3]
for(let value of numArray)
  console.log(value) // 1 2 3

const strArray: string[] = ["hello", "world", "!"]
for(let value of strArray)
  console.log(value) // hello world 1
